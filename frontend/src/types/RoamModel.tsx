export default interface RoamModel {
        id: string,
        pc: string,
        city: string, 
        street_name: string,
        street_number: number, 
        owner_id: number,
        charger_type: string,
        latitude: number, 
        longitude: number, 
        available: boolean
}
